const NavegacionEstilos = (theme) => ({
  avatar: {
    margin: theme.spacing(1),
    backgroundColor: 'transparent',
    width: theme.spacing(15),
    height: theme.spacing(15),
    cursor: 'pointer',
  },
  textItemAlign: {
    textAlign: 'center',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },

});

export default NavegacionEstilos;
