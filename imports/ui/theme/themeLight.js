import { createMuiTheme } from '@material-ui/core';

const themeLight = createMuiTheme({
  palette: {
    background: {
      default: '#ABD8CD',
    },
    backgroundColor: {
      main: '#ABD8CD',
    },
  },
});

export default themeLight;
